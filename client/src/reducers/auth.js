// initialstate
const initialState = {
  isAuthenticated: false,
  token: "",
  error: "",
};

// Reducer
const authReducer = (state = initialState, action) => {
  return state;
};

// Exporting reducer
export default authReducer;
